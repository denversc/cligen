This directory contains tests that are run against each target language.
The same argument specification file (a.k.a. "cligen.xml") is compiled for
each target language and is expected to compile and produce identical output.
